# Agent File Registry

Auto-managed by `/parallel` Step 3 (Spec 041 — Cross-Agent Conflict Detection).
Records file ownership per agent during parallel execution. Cleared after each `/parallel` run.

---

<!-- File registry written by /parallel step 3; cleared after merge (step 12) -->
<!-- Format:

# Agent File Registry — YYYY-MM-DD HH:MM

| File | Claimed by | Spec | Conflict resolution |
|------|-----------|------|---------------------|
| path/to/file.md | Spec NNN agent | NNN | worktree / halt / queue |

-->
