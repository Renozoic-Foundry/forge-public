# Scratchpad — Open Notes

Notes added via `/note`. Reviewed at `/close`, `/session`, and `/outer-loop`.

Tags: `[validate]` = next validate run | `[session]` = next session close | `[outer-loop]` = next outer loop review | (untagged) = next validate or session, whichever comes first.

---

