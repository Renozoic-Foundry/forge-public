# Spec 378 — exclude the gate-comparison defect corpus from GitHub secret scanning.
# These fixtures use realistic credential patterns by design (Spec 277 benchmark
# requirement). They are detection-test fixtures, not real secrets.
paths-ignore:
  - "template/scripts/gate-comparison-defects/**"
