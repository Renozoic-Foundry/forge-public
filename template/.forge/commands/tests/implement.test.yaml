command: implement
type: slash-command

structure:
  required_sections:
    - "## "
  required_strings:
    - "spec"
    - "GATE"
    - "PASS"
    - "FAIL"
    - "backlog.md"
    - "Chain-Next"
    - "/close"
  forbidden_strings:
    - "TODO"
    - "FIXME"
  max_lines: 600
  min_lines: 50

references:
  files_must_exist:
    - "docs/specs/_template.md"
    - "docs/specs/README.md"
    - "docs/specs/CHANGELOG.md"
    - "docs/backlog.md"
    - "docs/process-kit/choice-block.md"
  commands_must_exist:
    - "close"
    - "spec"
    - "tab"

template_vars:
  must_not_contain_raw:
    - "cookiecutter."


frontmatter:
  required_fields:
    - "name"
    - "description"
    - "workflow_stage"
  expected_values:
    name: "implement"
    workflow_stage: "implementation"
